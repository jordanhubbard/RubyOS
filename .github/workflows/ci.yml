name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

env:
  RUBYOS_BUILD_JOBS: "3"
  RUBYOS_REQUIRE_MEDIA: "1"
  SDL_VIDEODRIVER: dummy
  SDL_AUDIODRIVER: dummy

jobs:
  linux:
    name: Linux ${{ matrix.arch }} build and release
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: arm64
            runner: ubuntu-24.04-arm
          - arch: x86_64
            runner: ubuntu-24.04
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 180
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          submodules: recursive

      - name: Bootstrap host and toolchain
        run: make install

      - name: Validate Linux build and release targets
        env:
          RUBYOS_VALIDATE_PLATFORM: linux
        run: ./scripts/validate-release.sh

      - name: Upload Linux release bundle
        uses: actions/upload-artifact@v4
        with:
          name: rubyos-linux-${{ matrix.arch }}
          path: dist/*
          if-no-files-found: error
          compression-level: 0
          retention-days: 14

  macos:
    name: macOS build and release
    runs-on: macos-15
    timeout-minutes: 90
    env:
      HOMEBREW_NO_AUTO_UPDATE: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          submodules: recursive

      - name: Bootstrap hosted dependencies
        run: RUBYOS_SKIP_DOCKER=1 make install

      - name: Validate macOS build and release targets
        env:
          RUBYOS_VALIDATE_PLATFORM: macos
        run: ./scripts/validate-release.sh

      - name: Upload macOS release bundle
        uses: actions/upload-artifact@v4
        with:
          name: rubyos-macos
          path: dist/*
          if-no-files-found: error
          compression-level: 0
          retention-days: 14

  ci-ok:
    name: all-platforms
    needs: [linux, macos]
    if: always()
    runs-on: ubuntu-24.04
    steps:
      - name: Require Linux and macOS
        env:
          LINUX_RESULT: ${{ needs.linux.result }}
          MACOS_RESULT: ${{ needs.macos.result }}
        run: |
          test "$LINUX_RESULT" = success
          test "$MACOS_RESULT" = success
